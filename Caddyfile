{
    auto_https off
    order rate_limit before reverse_proxy
}

:{$CADDY_PORT} {
    rate_limit {
        zone api_limit {
            key {remote_host}
            events 10
            window 1m
        }
    }
    reverse_proxy localhost:4000
}
